import React from "react";
import { createRoot } from "react-dom/client";
import "./Index.scss";

const element : any = document.getElementById("root")
createRoot(element).render(<div>Hi</div>);
